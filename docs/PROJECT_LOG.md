# 项目日志

## 基本信息

- 项目名称：`yeah-boot-admin`
- 记录日期：2026-04-03
- 当前阶段：前端后台原型阶段，准备向高规格开源脚手架演进

## 项目背景

当前项目最初参考了若依体系的后台前端思路，但在实际使用过程中，现有框架的开发体验、可维护性和长期演进能力不符合预期。因此，本项目将转向自研，并以开源项目标准重新设计和建设。

本项目的目标不是做一个能跑的后台页面集合，而是沉淀一套可长期维护、可扩展、可复用、可开源协作的前后端脚手架体系。

## 核心目标

### 产品目标

- 打造一套自研的后台管理脚手架，替代原先基于若依的前端方案
- 项目应具备开源发布能力，而不是仅服务于单个私有业务
- 在功能设计上兼顾通用后台场景与二次开发效率

### 工程目标

- 高可维护性：模块边界清晰，目录职责明确，避免页面文件无限膨胀
- 高一致性：代码风格、命名、错误处理、状态管理、接口调用方式统一
- 高可测试性：核心逻辑可单测，关键流程可集成验证
- 高可扩展性：权限、菜单、布局、表格、表单、字典、请求层等均可演进
- 高可读性：新加入的开发者能在较短时间内理解项目结构和开发约定
- 高稳定性：基础设施改动要小心，避免“功能越来越多，项目越来越乱”

### 文档目标

- 文档必须完善，不能等项目做完再补
- 文档应覆盖：项目定位、架构设计、目录规范、开发规范、提交流程、发布流程、常见问题
- 关键设计决策应留痕，避免后续会话或后续开发时反复推翻
- 项目内必须保留持续更新的开发日志，确保跨会话、跨阶段可追溯

## 项目原则

- 先定规则，再写功能
- 先做基础设施，再铺业务页面
- 优先抽象稳定模式，不急于堆页面数量
- 能复用的不重复写，能约束的不用口头约定
- 复杂度要集中在基础层，不要散落在每个页面中
- 所有重要改动都应配套文档和日志记录

## 建议的工程建设方向

### 一、先收敛架构，再继续铺页面

当前仓库已经有后台页面原型，但整体仍偏静态演示。后续不建议继续直接堆业务页，建议先完成以下基础层设计：

- 路由体系：静态路由、权限路由、菜单映射、面包屑、页面标题的统一方案
- 布局体系：头部、侧边栏、标签页、内容区、主题与响应式行为的统一状态管理
- 请求体系：统一的 HTTP 客户端、错误处理、鉴权、重试、取消请求、接口类型定义
- 状态体系：明确哪些进入 Pinia，哪些留在页面内，哪些抽成 composable
- 组件体系：表格、搜索栏、弹窗表单、状态切换、字典显示等后台高频组件标准化
- 权限体系：菜单权限、按钮权限、路由权限、资源标识统一建模

### 二、建立“规范优先”的仓库约束

项目若要长期维护，建议尽快补齐以下约束：

- 目录规范文档
- 命名规范文档
- 组件设计规范
- API 封装规范
- 提交规范
- 代码评审检查清单
- 发布前检查清单

### 三、文档分层建设

建议后续逐步形成以下文档结构：

- `README.md`：项目介绍、快速开始、文档入口
- `docs/PROJECT_LOG.md`：项目背景、目标、阶段记录、关键决策摘要
- `docs/ARCHITECTURE.md`：整体架构和模块关系
- `docs/CONTRIBUTING.md`：协作与提交流程
- `docs/CODE_STYLE.md`：代码规范和示例
- `docs/CHANGELOG.md`：面向版本发布的变更记录

### 四、开发日志必须制度化

开发日志不应只是随手备注，建议固定记录以下内容：

- 日期
- 本次目标
- 已完成事项
- 遇到的问题
- 当前决策
- 下一步动作

后续每次进入新会话时，都应优先查看本文件，再继续推进工作。

## 质量标准

后续功能建设默认按以下标准推进：

- 页面逻辑尽量拆分，避免单文件组件承载过多职责
- 类型定义要完整，不依赖大面积 `any`
- 重复 UI 交互要抽成可复用组件或 composable
- 所有基础能力要有明确边界和统一入口
- 核心基础模块变更时，同步更新文档
- 每次阶段性改造都要记录到项目日志

## 建议的近期里程碑

### M1：基础规范落地

- 重写 README
- 建立 docs 目录
- 建立项目日志、架构文档、代码规范文档
- 明确目录分层、状态管理、请求封装和路由设计原则

### M2：后台基础设施重构

- 收敛布局状态管理
- 统一菜单与路由建模
- 建立 API 请求层与鉴权处理
- 抽象后台高频通用组件

### M3：业务模块标准化

- 以用户、角色、资源、组织等模块作为样板
- 统一列表页、搜索区、弹窗表单、权限控制模式
- 清理演示性质代码和模板残留

### M4：开源化准备

- 完善文档
- 完善示例
- 增加测试与质量门禁
- 梳理版本发布、贡献指南、许可证说明

## 当前明确决策

- 不再沿用若依式前端方案作为长期基础
- 新项目按高规格开源脚手架标准推进
- 文档与开发日志从现在开始纳入正式工程产物
- 后续工作应优先建设基础设施和规范，而非继续无约束堆页面

## 下一步建议

优先顺序建议如下：

1. 明确目标用户：该脚手架主要服务“个人项目 / 中小团队 / 企业内部后台”中的哪一类
2. 输出第一版架构文档：路由、权限、请求、状态、组件分层
3. 制定目录规范和页面开发模板
4. 开始对现有布局和路由做第一轮重构
5. 用一个模块作为标准样板页，验证规范是否足够好用

## 开发日志约定

从本次开始，后续开发记录建议按以下格式持续追加：

### YYYY-MM-DD

- 本次目标：
- 完成内容：
- 关键决策：
- 遗留问题：
- 下一步：

## 本次记录

### 2026-04-03

- 本次目标：明确项目定位，并将长期目标和工程要求固化到仓库文档
- 完成内容：确认项目要从“若依替代品”升级为“高规格自研开源脚手架”；新增项目日志并记录核心目标、原则、里程碑和开发日志制度
- 关键决策：后续开发优先级应调整为“规范与基础设施先行”
- 遗留问题：尚未形成正式架构文档、代码规范文档、贡献指南和版本变更记录
- 下一步：补齐 `README` 项目入口说明，并开始梳理第一版架构和目录规范

### 2026-04-03（架构草案）

- 本次目标：输出第一版前端架构文档，作为后续开发和重构的基线
- 完成内容：新增 `docs/ARCHITECTURE.md`，明确项目分层、目录目标、路由权限统一模型、请求层职责、状态管理边界、组件分层、测试门禁和迁移路径
- 关键决策：采用“应用层 + 核心层 + 共享层 + 业务模块层 + 页面层”的架构；菜单、路由、权限改为统一建模；Pinia 仅承载全局共享状态
- 遗留问题：目标用户画像仍是工作假设；目录规范、代码规范、贡献指南仍未落地
- 下一步：编写 `docs/CODE_STYLE.md`，把命名、目录、组件、API、状态、提交规范具体化

### 2026-04-03（菜单与页面边界定稿）

- 本次目标：明确脚手架项目在菜单、路由和业务页面上的长期策略
- 完成内容：
  - 确认采用“动态菜单 + 动态权限 + 固定内置页面 + 占位兜底”的方案
  - 明确用户管理、角色管理、菜单管理、组织管理、岗位管理、字典管理、系统日志属于脚手架内置模块
  - 明确商城等具体业务域页面不属于脚手架内置能力，只允许通过动态菜单或占位页方式承接
  - 已将上述原则固化到 `docs/ARCHITECTURE.md`
- 关键决策：
  - 脚手架不走“全固定预埋菜单”，也不走“全动态页面生成”
  - 后端新增业务菜单允许显示，但默认不进入主仓库正式页面实现范围
- 遗留问题：
  - 动态菜单对应的占位页、动态路由和权限收口仍需继续完善
- 下一步：
  - 继续按这套边界推进权限、菜单和通用后台模块建设

### 2026-04-03（范围澄清）

- 本次目标：根据用户补充信息，澄清前端项目的近期建设范围
- 完成内容：确认存在一个已成熟、已在实际工作中使用的开源后端服务项目；前端管理端 `v1` 的首要目标调整为“优先接通现有后端接口”
- 关键决策：前端项目近期不以抽象完整全新后端协议为目标，而以适配现有后端 API 为核心任务；架构设计仍保留高可维护标准，但优先级服从接口对接落地
- 遗留问题：尚未获得后端仓库地址、本地代码路径、OpenAPI/Swagger 文档及关键接口返回结构
- 下一步：获取后端仓库代码或公开地址，只读分析 Controller、DTO、权限与认证相关业务代码，并梳理前端接口对接清单

### 2026-04-03（后端契约接入）

- 本次目标：基于本地 Swagger 与后端仓库，开始将当前前端项目接入真实后端接口
- 完成内容：
  - 已使用 `.codex/swagger-all.json`、`.codex/yeah-boot`、`.codex/yeah-boot-ui` 作为接口与旧前端参考来源
  - 已确认管理端核心接口前缀以 `/admin/...` 为主，退出登录为 `/auth/logout`
  - 已确认管理端登录真实契约为 `username/password/key/code/terminal=web`
  - 已在当前项目新增统一请求层、token 存储、`auth/app` store、登录验证码接入、当前用户与个人资源菜单接入
  - 已将用户、角色、资源、岗位页面切换为真实查询接口
  - 已将组织架构页面切换为真实组织树与详情接口
  - 已通过 `npm run type-check` 与 `npm run build-only`
- 关键决策：
  - 不直接盲信 Swagger 中同名 DTO 的 schema 结果，优先以真实 Controller 与 DTO 源码校准
  - 新前端以现有后端 admin 契约为准，不再沿用原型中的本地字段命名
  - 先接“认证 + 菜单 + 查询”，再逐步推进完整 CRUD
- 遗留问题：
  - 当前各业务页的新增、编辑、删除仍未全部切到真实后端接口
  - 用户、角色、资源、组织等弹窗表单仍保留一部分原型阶段字段，尚未完全按后端 DTO 收敛
  - 资源树、按钮权限和完整动态路由仍是最小落地版本，后续仍需增强
- 下一步：
  - 继续把用户、角色、资源、组织、岗位的创建/更新/删除切到真实后端
  - 补齐 DTO 对齐后的表单结构
  - 逐步完善按钮权限、菜单适配和动态路由策略

### 2026-04-03（核心 CRUD 接入）

- 本次目标：在已接通认证、菜单与查询接口的基础上，继续接通核心管理页的真实 CRUD
- 完成内容：
  - 已补齐 `src/api/upms.ts` 中用户、角色、资源、组织、岗位的详情、创建、更新、删除及角色资源授权接口
  - 已将用户管理页表单字段按后端 DTO 收敛为 `nickname/mobile/gender/orgIds/postIds/roleIds/status/remark`
  - 已将角色管理页表单字段按后端 DTO 收敛为 `code/name/remark/status`，并接通角色资源授权提交
  - 已将资源管理页表单字段按后端 DTO 收敛为 `parentId/type/path/component/icon/permission/isLink/linkUrl/visible/sort/status/remark`
  - 已将岗位管理页切到真实详情、创建、更新、删除接口
  - 已将组织架构页切到真实组织创建、更新、删除，并用真实用户查询填充组织成员列表
  - 已再次通过 `npm run type-check` 与 `npm run build-only`
- 关键决策：
  - 先确保核心模块“可查、可增、可改、可删”闭环成立，再考虑更细的交互优化
  - 组织页详情与表单以真实后端字段为准，不再保留原型阶段的 `code/manager/phone` 假字段
  - 角色资源授权继续独立于角色基础信息表单，避免职责混杂
- 遗留问题：
  - 动态路由与资源树适配仍是最小可用方案，尚未做完整的前端页面映射策略
  - 组织页的“添加成员 / 移除成员”、资源页的更多辅助字段校验与体验仍可继续完善
  - 当前构建仍提示主包体积较大，后续需要做分包优化
- 下一步：
  - 联调真实后端运行环境，逐页验证 CRUD 行为与返回数据
  - 根据联调结果修正表单字段、参数序列化和异常处理细节
  - 继续完善权限按钮控制与动态路由生成策略

### 2026-04-03（联调修正：跨域与密码格式）

- 本次目标：修正本地联调过程中暴露的登录失败问题
- 完成内容：
  - 已确认开发环境登录失败并非只有接口逻辑问题，还包含浏览器 `CORS Missing Allow Origin`
  - 已在 `vite.config.ts` 中为 `/api` 增加本地代理到 `http://127.0.0.1:8080`
  - 已将请求层开发环境默认基地址切换为 `/api`，避免浏览器直接跨域请求后端
  - 已确认管理端登录和新增用户密码需要前端先做 `SHA-256`
  - 已在登录页和用户密码相关提交逻辑中加入 `SHA-256` 处理
- 关键决策：
  - 开发环境优先通过前端代理解决跨域，不要求后端先额外放开 CORS
  - 密码传输格式以现有后端与旧前端的实际契约为准，即前端先做 `SHA-256`
- 遗留问题：
  - 运行中的真实联调仍需要用户本地重新启动前端开发服务以加载新的 Vite 代理配置
  - 若后端部署地址未来变化，需要再抽出环境变量和多环境配置
- 下一步：
  - 重启前端 dev server 后重新验证登录
  - 如登录通过，再继续检查菜单、用户、角色、资源、组织、岗位页面的真实联调效果

### 2026-04-03（联调修正：菜单路径适配）

- 本次目标：解决登录后进入系统但左侧菜单不显示的问题
- 完成内容：
  - 已确认 `mineResources` 返回的是若依式资源树，父节点路径为绝对路径，子节点路径为相对路径
  - 已确认后端菜单节点中存在 `menu/dept/post/dict` 等路径，但当前新前端实际页面路径为 `resource/org/position`
  - 已在 `src/stores/auth.ts` 中加入菜单路径归一化与映射逻辑，将后端资源树转换为当前项目可识别的前端菜单路径
- 关键决策：
  - 菜单适配层负责做后端资源路径到当前前端页面路径的映射，而不是要求后端立即按新前端路由命名
  - 当前先采用显式映射表保证联调可用，后续再考虑收敛为更通用的动态路由模型
- 遗留问题：
  - `dict` 等当前前端尚未实现的页面不会出现在菜单中
  - 菜单图标名称目前直接透传后端值，后续可能还需要做图标兼容映射
- 下一步：
  - 刷新页面确认菜单已显示
  - 继续验证菜单点击后的页面是否与当前前端页面正确对应

### 2026-04-03（字典管理与系统日志）

- 本次目标：补齐基础后台功能中的“字典管理”和“系统日志”
- 完成内容：
  - 已补充字典与系统日志的数据类型、接口封装和路由入口
  - 已新增 `src/views/system/DictManage.vue`，实现字典树展示、当前节点查看、新增根节点、新增子节点、编辑、删除
  - 已新增 `src/views/system/LogManage.vue`，实现系统日志分页查询与详情查看
  - 已将后端资源路径 `/system/dict` 映射到当前前端的 `/system/dict`
  - 已通过 `npm run type-check` 与 `npm run build-only`
- 关键决策：
  - 字典管理先按“树 + 基础 CRUD”方式落地，优先满足可维护和可联调
  - 系统日志先按“只读查询 + 详情”方式落地，不先做删除/清空等高风险操作
  - App 用户管理仍暂缓，不进入当前阶段
- 遗留问题：
  - 系统日志是否在菜单中显示仍取决于后端 `mineResources` 是否返回对应资源
  - 字典管理当前优先使用树接口，不追求一开始就完全复刻旧前端的分页字典数据模式
  - 主包体积仍然偏大，后续需要做代码分包优化
- 下一步：
  - 联调字典管理与系统日志页面的真实运行效果
  - 根据联调结果继续补齐未在菜单中暴露但已具备页面能力的模块
  - 之后再进入代码规范和文档体系的下一轮完善

### 2026-04-03（用户管理页体验优化）

- 本次目标：优化用户管理页的查询与结构交互，并将可复用逻辑同步到全局
- 完成内容：
  - 已在用户管理页左侧增加组织树，点击节点后右侧用户列表会按组织联动查询
  - 已在用户管理页搜索区增加创建时间范围查询，对接后端 `createDateBegin/createDateEnd`
  - 已修正搜索区状态选择器过窄的问题
  - 已将 Element Plus 全局切换为中文 locale，分页文案同步中文化
  - 已在请求层补充 GET 数组参数序列化，支持 `orgIds` 这类多值查询参数
  - 已将搜索表单中选择器和日期控件的宽度处理做成全局样式，自动作用于其他页面
- 关键决策：
  - “组织树联动列表”先在用户管理页落地，不强行复制到所有页面
  - “分页中文化”“搜索选择器宽度”“数组参数序列化”属于通用能力，已提升为全局处理
- 遗留问题：
  - 其他页面若后续也需要树形筛选，仍建议按业务场景逐页设计，不直接照搬
  - 当前构建体积仍偏大，后续需要做分包优化
- 下一步：
  - 联调用户管理页的组织树筛选、时间范围筛选与分页交互
  - 如联调稳定，再继续优化其他页面的检索和列表体验

### 2026-04-03（用户管理页联动与全局中文化）

- 本次目标：继续完善用户管理页筛选体验，并将适合复用的优化提升为全局能力
- 完成内容：
  - 已在用户管理页左侧落地组织树过滤，点击组织节点会通过 `orgIds` 联动查询用户列表
  - 已在用户管理页搜索区增加创建时间范围，参数对接 `createDateBegin/createDateEnd`
  - 已在请求层增加数组参数序列化，保证 `orgIds` 这类参数可稳定传输
  - 已将 Element Plus 全局切换为中文 locale，分页栏等内置文案已中文化
  - 已将搜索表单中选择器、日期范围控件的宽度处理抽到全局样式，对其他页面自动生效
  - 已再次通过 `npm run type-check` 与 `npm run build-only`
- 关键决策：
  - 组织树联动列表属于页面级交互，先在用户管理页落地，不强行抽成全局模板
  - 分页中文化、搜索选择器宽度、数组参数序列化属于全局通用能力，已统一处理
- 遗留问题：
  - 当前本地无法直接从当前执行环境访问你的后端运行实例，实际联调结果仍需在浏览器中确认
  - 若后端未来对数组参数有更严格的序列化要求，可能还需要按真实运行结果微调
- 下一步：
  - 继续根据你在浏览器中的联调反馈修正用户管理页的筛选与交互细节
  - 如果用户管理页稳定，再选择下一张高频页面做交互体验收口

### 2026-04-03（用户管理页弹窗修复）

- 本次目标：修复“新增用户”弹窗继承“编辑用户”旧数据的问题
- 完成内容：
  - 已将用户表单默认值提取为统一工厂函数
  - 已移除依赖 `resetFields()` 的重置方式，改为显式恢复默认值并清除校验状态
- 关键决策：
  - 对于复用型弹窗表单，优先使用“显式恢复默认值 + clearValidate”而不是依赖组件内部的初始值快照
- 遗留问题：
  - 其他页面若也存在类似“新增继承编辑旧值”的情况，需要按同样方式处理
- 下一步：
  - 继续根据页面联调反馈收口用户管理页细节

### 2026-04-03（用户管理页重置密码交互）

- 本次目标：优化用户管理页“重置密码”的交互方式
- 完成内容：
  - 已将“重置密码”从固定重置为默认值，改为弹出密码输入框
  - 已要求输入的新密码长度至少为 6 位
  - 提交前仍按现有后端契约对密码进行 `SHA-256`
  - 已通过 `npm run type-check`
- 关键决策：
  - 重置密码必须由操作者显式输入新密码，避免硬编码默认密码带来的安全与使用问题
- 遗留问题：
  - 后续若需要更严格的密码规则，可再追加复杂度校验
- 下一步：
  - 继续根据联调反馈收口用户管理页细节

### 2026-04-03（用户管理页列表与详情分层）

- 本次目标：继续优化用户管理页信息呈现方式
- 完成内容：
  - 已将列表中的“电话”语义统一为“手机号”
  - 已将“岗位”“备注”加入用户列表展示
  - 已将“邮箱”“手机号”从列表移动到“详情”弹窗中展示
  - 已在操作列增加“详情”按钮
  - 已通过 `npm run type-check`
- 关键决策：
  - 列表优先展示高频概览字段，联系信息类字段移入详情弹窗，降低列表横向拥挤
- 遗留问题：
  - 若后续还需在列表中展示更多字段，应优先考虑详情弹窗或列显隐控制，而不是继续堆列
- 下一步：
  - 继续根据联调反馈收口用户管理页展示与交互

### 2026-04-03（菜单权限修复）

- 本次目标：修正前端菜单权限与页面访问权限未按后端资源勾选状态生效的问题
- 完成内容：
  - 已修正菜单树适配逻辑，前端现在只显示 `mineResources` 中 `checked = true` 的页面节点，以及其必要父级节点
  - 已在路由守卫中加入页面访问校验，未授权的 `/system/*` 页面不能再仅靠手输 URL 访问
  - 已通过 `npm run type-check`
- 关键决策：
  - 菜单与页面访问权限当前统一以 `mineResources` 返回的 `checked` 状态为准
- 遗留问题：
  - 按钮权限当前还无法做到完全准确，原因是后端 `mine()` 中的 `permissions` 目前写死为 `*:*:*`，且用户提供的 `mineResources` 示例只包含页面节点，不包含按钮节点
  - 若要把按钮权限也做准，后端需要返回可区分的按钮级权限数据，或修正 `mine().permissions`
- 下一步：
  - 让用户刷新后用 test 用户验证菜单只剩“字典管理”
  - 若按钮权限后续要严格控制，需要结合后端返回的真实按钮资源或真实权限字符串继续补齐

### 2026-04-03（菜单权限修正收紧）

- 本次目标：进一步修正菜单适配层中的页面可访问路径判断
- 完成内容：
  - 已移除父级菜单 `/system` 到具体页面路径的错误映射
  - 已调整可访问路径集合的生成逻辑，只把叶子页面节点视为真正可访问页面
  - 已通过 `npm run type-check`
- 关键决策：
  - 父级菜单只承担分组和展示职责，不参与页面放行判断
- 遗留问题：
  - 按钮权限仍受后端数据源限制，暂未完全收口
- 下一步：
  - 让用户刷新并验证 test 用户是否只剩“字典管理”菜单与页面访问权限

### 2026-04-03（查询条件改造）

- 本次目标：将前端文本类搜索从精确匹配改为后端约定的 `conditions_` 查询机制
- 完成内容：
  - 已新增查询条件拼装工具 `src/utils/query.ts`
  - 已将用户、角色、资源、岗位、系统日志页面中的文本搜索改为 `conditions_` 模糊查询
  - 状态、日期范围、组织树筛选等结构化参数继续保留原参数传递方式
- 关键决策：
  - “名称类/文本类”统一优先走 `conditions_=字段:like:值`
  - 非文本筛选项继续走明确字段参数，避免把全部查询都塞进 `conditions_`
- 遗留问题：
  - 其他后续新增页面若涉及文本搜索，也应遵循同样的 `conditions_` 拼装方式
- 下一步：
  - 根据联调结果继续修正具体字段的查询动作（如 `like` / `eq` / `between`）

### 2026-04-03（用户管理查询例外）

- 本次目标：按最新要求调整用户管理页的列表查询方式
- 完成内容：
  - 已将用户管理页的用户名查询从 `conditions_` 模糊查询改回直接参数 `username`
  - 其他页面仍保持 `conditions_` 方案，后续按实际联调需要逐页调整
- 关键决策：
  - `conditions_` 不做一刀切；允许按页面和后端实际查询语义保留例外
- 下一步：
  - 继续根据联调反馈逐页收口查询参数规则

### 2026-04-03（菜单管理树形展示）

- 本次目标：按最新要求将菜单管理从分页表格切换为树状全量展示
- 完成内容：
  - 已将菜单管理页面标题从“资源管理”调整为“菜单管理”
  - 已将列表数据源从分页查询切换为 `/admin/sysRes/tree`
  - 已将菜单列表改为树状表格展示，默认展开全部层级
  - 已移除分页栏及相关分页查询逻辑
  - 弹窗新增/编辑/删除逻辑继续保留
- 关键决策：
  - 菜单管理优先按树状全量展示实现，更符合菜单/资源层级的实际管理方式
- 遗留问题：
  - 当前资源类型筛选仍在前端树结果上做过滤，后续可根据联调结果决定是否完全交给后端树接口处理
- 下一步：
  - 联调菜单树展示、搜索、类型筛选和 CRUD 行为

### 2026-04-05（菜单图标选择器）

- 本次目标：优化菜单管理中“图标”字段的编辑方式
- 完成内容：
  - 已将菜单管理中的图标字段从手动输入改为可视化选择
  - 已新增轻量级图标选择组件 `src/components/layout/IconSelectPopover.vue`
  - 图标选择基于当前项目使用的 Element Plus 图标体系，不引入若依整套 svg-icon 依赖
- 关键决策：
  - 参考若依“图标选择”的交互方式，但在当前项目中采用更轻量、更一致的实现
- 遗留问题：
  - 若后续需要支持更多图标，可继续扩充候选图标列表
- 下一步：
  - 继续联调菜单管理的树展示与表单交互

### 2026-04-05（菜单管理勾选框清理）

- 本次目标：清理菜单管理页面中无实际用途的勾选框
- 完成内容：
  - 已移除菜单管理树表格中的勾选列
  - 已删除对应的无用选择状态和 `selection-change` 事件
- 关键决策：
  - 菜单管理当前不支持批量操作，保留勾选框只会增加干扰
- 下一步：
  - 继续按实际使用场景精简菜单管理页面交互

### 2026-04-05（菜单管理文案统一）

- 本次目标：统一菜单管理页面中“资源/菜单”相关文案
- 完成内容：
  - 已将父级选择文案调整为“上级菜单 / 顶级菜单”
  - 已将新增、编辑与成功提示文案统一为“菜单”
  - 已通过 `npm run type-check`

### 2026-04-05（菜单管理根菜单文案）

- 本次目标：对齐菜单管理中顶层菜单的用户认知
- 完成内容：
  - 已调整菜单管理中上级菜单选择的顶层选项文案，最终以“根菜单”为准

### 2026-04-05（树形顶层节点文案）

- 本次目标：统一树形管理页面中顶层节点的命名
- 完成内容：
  - 菜单管理顶层选项改为“根菜单”
  - 组织管理顶层选项改为“根组织”

### 2026-04-05（父级选项层级展示）

- 本次目标：增强树形管理页面编辑弹窗中父级选择的层级感知
- 完成内容：
  - 已将菜单管理和组织管理的父级选择从文本缩进列表切换为真正的 TreeSelect 结构
  - 现在“根菜单 / 根组织”作为根节点，子级以树形方式展开显示

### 2026-04-05（页面标题去重）

- 本次目标：移除系统页中与面包屑重复的主标题
- 完成内容：
  - 已移除用户管理、角色管理、菜单管理、岗位管理、组织管理、字典管理、系统日志页面中的重复 `h2` 标题
  - 已保留各页面顶部操作按钮区，并统一调整为右对齐布局

### 2026-04-05（父级节点数值化）

- 本次目标：进一步收紧树形管理页面中父级节点的表单赋值逻辑
- 完成内容：
  - 已将菜单管理与组织管理编辑时的 `parentId` 赋值改为显式数值化
  - 已在菜单管理新增时显式回写 `parentId = 0`
  - 已通过 `npm run type-check`

### 2026-04-03（动态菜单与占位路由）

- 本次目标：让后端新增的已授权菜单无需手工前端白名单也能显示
- 完成内容：
  - 已新增动态菜单占位页 `src/views/system/RoutePlaceholderView.vue`
  - 已在路由层加入动态菜单路由注册逻辑
  - 已取消菜单对白名单路径的强依赖，后端新增且 `checked=true` 的菜单现在可以显示
  - 对当前已实现页面继续使用显式组件映射，对未知后端组件先落到占位页，避免点击后直接 404
- 关键决策：
  - 对已实现模块继续走稳定映射；对未知模块先提供占位页，优先打通“菜单出现 + 可点击”
- 遗留问题：
  - 真正的新业务模块若要落地正式页面，仍需要前端补对应实现
  - 按钮权限仍未完全收口，仍受后端权限数据返回方式限制
- 下一步：
  - 让用户刷新并验证“商城管理”是否已显示
  - 若显示后点击进入占位页，再决定是否继续实现真实商城页面

### 2026-04-03（按钮权限前端收口）

- 本次目标：为核心页面接入统一的按钮权限判断能力
- 完成内容：
  - 已在认证状态层补充真实权限集合与页面动作判断能力
  - 已将用户、角色、菜单、岗位、字典、组织等页面的核心操作按钮接入统一权限判断
  - 当前策略为：若后端返回了按钮级资源，则严格按按钮资源控制；若页面下尚未配置按钮级资源，则默认继承页面权限，避免全部按钮误隐藏
- 关键决策：
  - 当前后端 `mine().permissions` 返回 `*:*:*` 不可信，按钮权限优先依赖资源树中的 `checked + permission`
  - 在后端按钮资源尚未完整配置前，前端采用“按钮资源优先、页面权限兜底”的兼容策略
- 遗留问题：
  - 若后端页面下存在按钮资源，但命名或 permission 与前端候选值不一致，仍可能出现按钮显示不准确
  - 最终想做到完全准确，仍建议后端返回稳定的按钮级权限数据
- 下一步：
  - 让用户按实际账号联调按钮显示结果
  - 根据真实资源命名进一步细化前端按钮候选权限映射

### 2026-04-05（按钮权限补齐）

- 本次目标：继续补齐前端页面中剩余的重要按钮权限控制点
- 完成内容：
  - 已为用户、角色、菜单、岗位列表中的状态开关补充权限控制，未授权时禁用
  - 已为组织管理页的导入、导出、添加成员、成员编辑、成员移除补充权限控制
  - 已为系统日志“详情”按钮补充权限控制
  - 已再次通过 `npm run type-check`
- 关键决策：
  - 搜索、重置、取消等非敏感通用交互不做按钮级权限控制
  - 弹窗“确定”按钮跟随上层新增/编辑入口控制，不单独再做一层权限判断
  - 用户详情按钮当前默认跟随页面访问权限，不单独拆成按钮权限
- 遗留问题：
  - `src/views/system/DeptManage.vue` 仍是遗留页，未继续补权限，不建议再投入
  - 若后端后续补齐真实按钮资源，需要再根据实际 `name/permission` 微调候选匹配规则
- 下一步：
  - 让用户按不同账号联调按钮显示效果
  - 若按钮显示与后端真实授权仍有偏差，再根据返回的按钮级资源继续修正

### 2026-04-05（组织管理树状重构）

- 本次目标：按最新要求将组织管理页面调整为树状管理，不再展示组织成员
- 完成内容：
  - 已移除组织管理页面中的组织成员区域与相关查询逻辑
  - 已将组织管理重构为树状表格全量展示，交互模式与菜单管理保持一致
  - 已保留组织的新增、编辑、删除及展开/折叠
  - 已继续沿用现有组织 CRUD 接口
  - 已通过 `npm run type-check`
- 关键决策：
  - 组织下的用户查询统一交给用户管理页面处理，组织管理只负责组织树本身的维护
- 遗留问题：
  - 暂无导入/导出能力，当前按需求不提供
- 下一步：
  - 继续联调整个组织树页面的搜索、树展示和 CRUD 行为

### 2026-04-05（组织管理查询例外）

- 本次目标：按后端 DTO 约定修正组织管理树查询参数
- 完成内容：
  - 已将组织管理树搜索从 `conditions_` 改回显式参数 `name/status`
  - 与后端 `SysOrgTreeQueryDto` 的 `LIKE/EQ` 注解语义保持一致
- 关键决策：
  - 若后端查询 DTO 已明确定义字段语义，则优先使用显式字段参数，不再强制走 `conditions_`
- 下一步：
  - 继续按后端 DTO 实际定义逐页收口查询参数规则

### 2026-04-05（顶级节点 parentId 修复）

- 本次目标：修复“顶级组织/顶级资源”提交时 `parentId=0` 被错误丢失的问题
- 完成内容：
  - 已修正组织管理提交参数，保留 `parentId: 0`
  - 已同步修正菜单管理提交参数，保留 `parentId: 0`
- 关键决策：
  - 对于以 `0` 表示顶级节点的树结构，前端提交时不能再将 `0` 视为“空值”
- 下一步：
  - 继续根据树形管理页面联调反馈修正参数细节

### 2026-04-05（主操作并入搜索区）

- 本次目标：将系统页中的主操作按钮与搜索区域合并到同一行
- 完成内容：
  - 已按统一规范将内容页整理为“搜索区域 -> 操作按钮区域 -> 内容区域”
  - 已将用户、角色、菜单、岗位、组织页面的主操作按钮移到独立操作按钮区域
  - 已将组织管理的“展开/折叠”放入操作按钮区域
  - 已将字典管理调整为无搜索区、仅操作按钮区域 + 内容区域的结构
  - 已通过 `npm run type-check`
- 关键决策：
  - 内容页优先遵循“搜索区域、操作按钮区域、列表内容区域”的三段式规范
  - 对于没有统一搜索表单的页面，使用独立操作按钮区域而不是强行拼接空搜索表单

### 2026-04-05（内容区域铺满）

- 本次目标：让系统页内容区域宽高尽量铺满可用空间，减少底部留白
- 完成内容：
  - 已将布局底座 `LayoutContent` 调整为 `100%` 高度并移除额外内边距
  - 已将系统页根容器统一改为占满内容区域
  - 已让表格/树等主内容区尽量填充剩余高度
- 关键决策：
  - 内容页统一按“占满内容区域”的方式组织，不再使用只包裹内容高度的外层页面壳

### 2026-04-05（搜索区去背景）

- 本次目标：统一搜索区域的视觉样式
- 完成内容：
  - 已去掉系统页搜索区域的背景色、圆角和额外内边距
  - 搜索区域现在与整个内容区保持同一视觉层级，只保留结构间距

### 2026-04-05（面包屑首页去重）

- 本次目标：修复面包屑中重复出现“首页”的问题
- 完成内容：
  - 已在公共头部面包屑生成逻辑中排除父路由 `/` 的重复“首页”节点
  - 修复后所有页面的面包屑都应从“首页 -> 当前页”开始，而不会再出现“首页 -> 首页 -> 当前页”

### 2026-04-05（面包屑父级补全）

- 本次目标：让面包屑正确展示父级菜单层级
- 完成内容：
  - 已将公共面包屑逻辑改为优先依据当前菜单树回溯父级节点
  - 已取消强制追加“首页”前缀，系统页现在应显示为“系统管理 -> 用户管理”这类层级结构
  - 已将无真实路由的分组节点处理为不可点击文本，避免点击后进入 404

### 2026-04-05（页面标签栏）

- 本次目标：为后台页面增加可切换的页签标签
- 完成内容：
  - 已新增公共页签组件 `src/components/layout/LayoutTabs.vue`
  - 进入页面后会自动生成页签标签
  - 点击标签可切换页面
  - 标签支持关闭；关闭当前标签时会自动切到相邻标签
  - 页签状态已挂到 `app store`
  - 退出登录时会自动清空页签

### 2026-04-18（字典树交互收口）

- 本次目标：优化字典管理左侧树的点击交互
- 完成内容：
  - 已将字典树改为仅点击箭头时展开/折叠
  - 点击节点名称时只执行查看/选中逻辑，不再触发展开收起

### 2026-04-18（字典树节点快捷操作）

- 本次目标：提升字典管理左侧树节点的操作效率
- 完成内容：
  - 已在左侧字典树每个节点上增加“新增 / 编辑 / 删除”快捷按钮
  - 已将节点操作从文字改为更紧凑的图标按钮
  - 已去掉圆形背景，仅保留简洁图标样式
  - 已改为仅在鼠标移入当前节点行时显示按钮，移出后隐藏
  - 已为左侧字典树增加横向/纵向滚动支持，层级深或节点多时都可滚动查看
  - 已对按钮点击做事件阻止，避免与节点选中、展开折叠行为冲突
  - 已通过 `npm run type-check`
